const frutas = ["maça", "uva", "pera", "melancia"];

// for in => lê os indices ou chaves do bjeto
// for(let i in frutas) {
//     console.log(frutas[i]);
// }

const pessoa = {
  nome: "Leonardo",
  sobrenome: "Henrique",
  idade: 15,
};

for (let i in pessoa) {
  console.log(i);
}
